//! # configure command
//!
//! This command removes configuration / data from MongoDB, Keycloak and S3.
//!

use crate::commands::RemoveCommand;

impl RemoveCommand {
    pub async fn run(self) -> anyhow::Result<()> {
        unimplemented!()
    }
}
